name: Build

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      verbose:
        description: Enable verbose logging for Nix builds
        required: false
        default: false
        type: boolean

jobs:
  linux:
    runs-on: ubuntu-latest
    env:
      VERBOSE_FLAGS: ${{ inputs.verbose == true && '--verbose --print-build-logs' || '' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - run: nix build --accept-flake-config .#nixosConfigurations.mothership.config.system.build.toplevel $VERBOSE_FLAGS

  arm-linux:
    # Validates each aarch64-linux agent-sandbox micro VM system closure on PRs.
    # The vfkit runners are aarch64-darwin and are exercised in the Cachix
    # workflow post-merge (they need the linux closures in cache first).
    runs-on: ubuntu-24.04-arm
    env:
      VERBOSE_FLAGS: ${{ inputs.verbose == true && '--verbose --print-build-logs' || '' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - run: nix build --accept-flake-config .#nixosConfigurations.agent-sandbox-damascus.config.system.build.toplevel $VERBOSE_FLAGS

      - run: nix build --accept-flake-config .#nixosConfigurations.agent-sandbox-MacBook-Pro-2.config.system.build.toplevel $VERBOSE_FLAGS

      # Extra aarch64-linux closures the darwin vfkit runners need.
      - run: nix build --accept-flake-config .#packages.aarch64-linux.agent-sandbox-damascus-runner-deps $VERBOSE_FLAGS

      - run: nix build --accept-flake-config .#packages.aarch64-linux.agent-sandbox-MacBook-Pro-2-runner-deps $VERBOSE_FLAGS

  arm-macos:
    runs-on: macos-26
    env:
      VERBOSE_FLAGS: ${{ inputs.verbose == true && '--verbose --print-build-logs' || '' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install Nix
        uses: cachix/install-nix-action@13d8dd58da0234aa297dedd986986ccb8e7f3e24 # v31.11.1
        with:
          nix_path: nixpkgs=channel:nixpkgs-unstable

      - run: nix build --accept-flake-config .#darwinConfigurations.MacBook-Pro-2.config.system.build.toplevel $VERBOSE_FLAGS

      - run: nix build --accept-flake-config .#darwinConfigurations.damascus.config.system.build.toplevel $VERBOSE_FLAGS
