# Encrypted credentials

These YAML files contain SOPS-encrypted values. Recipient rules are in
`.sops.yaml`; private identities and plaintext credentials stay outside Git.

See [credential management](../notes/credentials.md) for declarations, editing,
enrollment, rotation, and recovery.
